name: ramo_nessa_passenger
description: Aplicativo Flutter do passageiro Ramo Nessa.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_map: ^8.3.2
  geolocator: ^14.0.3
  http: ^1.6.0
  latlong2: ^0.10.1
  ramo_design_system:
    path: ../../packages/design_system

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
